services:
  api:
    build:
      context: .
      dockerfile: backend/ShareService.Api/Dockerfile
    ports:
      - "5000:8080"
    env_file:
      - .env
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
