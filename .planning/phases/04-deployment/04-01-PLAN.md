---
phase: 04-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fly.toml
  - scripts/verify-deploy.ps1
  - README.md
autonomous: true
requirements: [OPS-04, OPS-05]
user_setup:
  - service: fly.io
    why: "Create and deploy the production app"
    env_vars:
      - name: Mongo__ConnectionString
        source: "MongoDB Atlas Connect dialog (SRV URI)"
      - name: Mongo__DatabaseName
        source: "Project database name chosen by operator"
      - name: Auth__JwtSecret
        source: "Generated locally via command in README"
      - name: Cors__AllowedOrigins
        source: "Derived from Fly app URL: https://<app>.fly.dev"
    dashboard_config:
      - task: "Authenticate Fly CLI and create app"
        location: "Terminal: fly auth login, fly launch --no-deploy"
  - service: mongodb-atlas
    why: "Provide managed production database connectivity"
    env_vars:
      - name: Mongo__ConnectionString
        source: "Atlas connection string (mongodb+srv://...)"
    dashboard_config:
      - task: "Allow network access from Fly for v1"
        location: "Atlas Dashboard -> Network Access -> 0.0.0.0/0"
must_haves:
  truths:
    - "Operator can deploy API to Fly.io with a committed manifest and no manual config-file edits beyond app-specific values."
    - "Operator can set required production secrets with exact key names before first deploy."
    - "Operator can verify deployment with health and authenticated upload checks using copy-paste commands."
    - "Operator can diagnose common failures (secret mismatch, CORS mismatch, Atlas auth/network) from README guidance."
  artifacts:
    - path: "fly.toml"
      provides: "Fly deployment manifest with always-on single instance, region fra, HTTPS, and /health check"
      contains: "primary_region = \"fra\""
    - path: "scripts/verify-deploy.ps1"
      provides: "Repeatable deployment smoke verification (health + upload)"
      contains: "Invoke-RestMethod"
    - path: "README.md"
      provides: "Zero-to-deploy Fly + Atlas runbook and troubleshooting"
      contains: "fly auth login"
  key_links:
    - from: "fly.toml"
      to: "backend/ShareService.Api/Dockerfile"
      via: "[build].dockerfile path"
      pattern: "dockerfile\\s*=\\s*\"backend/ShareService.Api/Dockerfile\""
    - from: "fly.toml"
      to: "backend/ShareService.Api/Program.cs"
      via: "http_service internal port and /health check"
      pattern: "internal_port\\s*=\\s*8080|path\\s*=\\s*\"/health\""
    - from: "README.md"
      to: "Fly runtime secrets"
      via: "fly secrets set command using ASP.NET env key format (__)"
      pattern: "Mongo__ConnectionString|Auth__JwtSecret|Cors__AllowedOrigins"
---

<objective>
Create deployment-ready Fly.io configuration and operator runbook so the team can deploy this API to free hosting with MongoDB Atlas and verify it immediately.

Purpose: Close OPS-04 and OPS-05 by turning existing runtime behavior (`/health`, env-driven config, Dockerfile) into a deterministic production deployment path.
Output: Committed `fly.toml`, reusable deployment verification script, and README zero-to-deploy documentation with troubleshooting.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment/04-CONTEXT.md
@.planning/phases/04-deployment/04-RESEARCH.md
@.env.example
@backend/ShareService.Api/Program.cs
@backend/ShareService.Api/Dockerfile
@README.md

<interfaces>
From backend/ShareService.Api/Program.cs:
```csharp
var mongoConnectionString = Require("Mongo:ConnectionString");
var mongoDatabaseName = Require("Mongo:DatabaseName");
var jwtSecret = Require("Auth:JwtSecret");
var allowedOriginsRaw = Require("Cors:AllowedOrigins");
app.MapGet("/health", async (IMongoClient client) => { ... });
```

From .env.example (production keys must use `__` in Fly secrets):
```env
Mongo__ConnectionString=
Mongo__DatabaseName=
Auth__JwtSecret=
Cors__AllowedOrigins=
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Fly deployment manifest aligned to locked decisions</name>
  <read_first>
    - .planning/phases/04-deployment/04-CONTEXT.md
    - .planning/phases/04-deployment/04-RESEARCH.md
    - backend/ShareService.Api/Program.cs
    - backend/ShareService.Api/Dockerfile
  </read_first>
  <files>fly.toml</files>
  <action>Create `fly.toml` at repo root with a single always-on API instance configuration: `primary_region="fra"`, Dockerfile build path `backend/ShareService.Api/Dockerfile`, `http_service.internal_port=8080`, `force_https=true`, `auto_stop_machines=false`, `auto_start_machines=true`, `min_machines_running=1`, and one HTTP health check against `/health`. Include concise comments clarifying that production secrets are injected via `fly secrets set` and must use `__` key names. Do not add autoscaling, multi-region, or scale-to-zero settings (deferred ideas).</action>
  <acceptance_criteria>
    - `fly.toml` exists at repository root.
    - `fly.toml` contains `primary_region = "fra"`.
    - `fly.toml` contains `internal_port = 8080`, `force_https = true`, and `/health` check path.
    - `fly.toml` contains always-on settings (`auto_stop_machines = false`, `min_machines_running = 1`).
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "Test-Path fly.toml; Select-String -Path fly.toml -Pattern 'primary_region\\s*=\\s*\"fra\"','internal_port\\s*=\\s*8080','force_https\\s*=\\s*true','auto_stop_machines\\s*=\\s*false','min_machines_running\\s*=\\s*1','path\\s*=\\s*\"/health\"' | Measure-Object | ForEach-Object { $_.Count }"</automated>
  </verify>
  <done>`fly.toml` can be used with `fly launch --no-deploy` + `fly deploy` without manual manifest edits beyond app-specific naming values.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable post-deploy smoke verification script</name>
  <read_first>
    - .planning/phases/04-deployment/04-RESEARCH.md
    - backend/ShareService.Api/Program.cs
    - scripts/start-local.ps1
  </read_first>
  <files>scripts/verify-deploy.ps1</files>
  <action>Create `scripts/verify-deploy.ps1` that accepts app URL and bearer token parameters, checks `GET /health`, then runs authenticated `POST /documents` with a minimal valid JSON payload, and confirms response includes `id` + `url`. Include clear non-zero exit on failure and explicit error messages for health failure, auth failure, and payload rejection. Keep script platform-local (PowerShell only) and do not embed secrets in file defaults.</action>
  <acceptance_criteria>
    - `scripts/verify-deploy.ps1` exists and is parameterized (no hardcoded secret values).
    - Script contains health probe for `/health`.
    - Script contains authenticated upload probe for `/documents` with `Authorization: Bearer`.
    - Script exits non-zero on failed API checks.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "Test-Path scripts/verify-deploy.ps1; Select-String -Path scripts/verify-deploy.ps1 -Pattern 'param\\s*\\(','/health','/documents','Authorization\\s*=\\s*\"Bearer','exit\\s+1|throw' | Measure-Object | ForEach-Object { $_.Count }"</automated>
  </verify>
  <done>Operators can run one command to validate a deployed instance instead of manually composing ad-hoc API checks.</done>
</task>

<task type="auto">
  <name>Task 3: Write zero-to-deploy README runbook with troubleshooting and security tradeoff note</name>
  <read_first>
    - README.md
    - .planning/phases/04-deployment/04-CONTEXT.md
    - .planning/phases/04-deployment/04-RESEARCH.md
    - .env.example
  </read_first>
  <files>README.md</files>
  <action>Add a dedicated "Fly.io Deployment (Phase 4)" section with copy-paste command flow: install/auth (`fly auth login`), bootstrap app (`fly launch --no-deploy`), generate JWT secret examples (PowerShell + bash), set required secrets with exact keys (`Mongo__ConnectionString`, `Mongo__DatabaseName`, `Auth__JwtSecret`, `Cors__AllowedOrigins=https://&lt;app&gt;.fly.dev`), run `fly deploy`, then verify via `scripts/verify-deploy.ps1` and direct `/health` checks. Include Atlas v1 guidance to use `0.0.0.0/0` allowlist with explicit security tradeoff and future hardening note. Add concise troubleshooting subsection for bad secret key names, CORS mismatch, and Atlas auth/network issues. Avoid adding deferred topics like autoscaling or strict Fly egress allowlisting.</action>
  <acceptance_criteria>
    - README contains explicit `fly auth login`, `fly launch --no-deploy`, `fly secrets set`, and `fly deploy` commands.
    - README documents all required production env keys with `__` naming.
    - README includes explicit production CORS value pattern `https://&lt;app&gt;.fly.dev`.
    - README includes Atlas `mongodb+srv://` requirement and `0.0.0.0/0` tradeoff note.
    - README includes troubleshooting entries for secret key mismatch, CORS mismatch, and Atlas connectivity/auth.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "Select-String -Path README.md -Pattern 'fly auth login','fly launch --no-deploy','fly secrets set','fly deploy','Mongo__ConnectionString','Auth__JwtSecret','Cors__AllowedOrigins','https://<app>\\.fly\\.dev','mongodb\\+srv://','0\\.0\\.0\\.0/0','troubleshooting','/health','verify-deploy\\.ps1' | Measure-Object | ForEach-Object { $_.Count }"</automated>
  </verify>
  <done>README becomes a deterministic operator runbook that takes a new teammate from zero to validated deployment with no configuration guesswork.</done>
</task>

</tasks>

<verification>
- `dotnet test v5-coordinate-routing.sln -v minimal` passes before and after deployment-doc/config updates.
- `pwsh -NoProfile -Command "Test-Path fly.toml; Test-Path scripts/verify-deploy.ps1"` returns True for both files.
- README grep checks confirm all required deployment anchors exist.
</verification>

<success_criteria>
- OPS-04: A committed `fly.toml` exists and encodes the required deployment shape (`fra`, always-on single machine, HTTPS, `/health`, internal port 8080).
- OPS-05: README contains complete Fly + Atlas deployment runbook, secret generation guidance, required env mapping, verification flow, and troubleshooting.
- Team can execute commands in README and reach a healthy deployed endpoint without undocumented manual steps.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment/04-deployment-01-SUMMARY.md`
</output>
