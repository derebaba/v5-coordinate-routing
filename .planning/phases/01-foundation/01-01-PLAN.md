---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/ShareService.Api/ShareService.Api.csproj
  - backend/ShareService.Api/Program.cs
  - backend/ShareService.Api/Dockerfile
  - backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj
  - backend/ShareService.Api.Tests/HealthTests.cs
  - backend/ShareService.Api.Tests/ConfigTests.cs
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - "The API only starts when Mongo settings, JWT secret, and allowed origins are supplied through environment variables."
    - "GET /health returns 200 and proves the API can reach MongoDB with the configured connection."
    - "The backend foundation preserves the existing frontend assumption that the API is hosted on http://localhost:5000."
  artifacts:
    - path: "backend/ShareService.Api/Program.cs"
      provides: "ASP.NET Core 8 startup with fail-fast env binding, UseStaticFiles, CORS, Mongo registration, and /health"
      contains: "app.MapGet(\"/health\""
    - path: "backend/ShareService.Api/ShareService.Api.csproj"
      provides: "API project with MongoDB.Driver dependency"
      contains: "MongoDB.Driver"
    - path: "backend/ShareService.Api.Tests/HealthTests.cs"
      provides: "Health endpoint verification against a temporary MongoDB instance"
      contains: "/health"
    - path: "backend/ShareService.Api.Tests/ConfigTests.cs"
      provides: "Missing-config startup validation"
      contains: "Missing required config"
  key_links:
    - from: "backend/ShareService.Api/Program.cs"
      to: "MongoDB"
      via: "MongoClient created from Mongo__ConnectionString and pinged in /health"
      pattern: "Mongo__ConnectionString"
    - from: "backend/ShareService.Api/Program.cs"
      to: "CORS policy"
      via: "Cors__AllowedOrigins is split and passed to WithOrigins(...)"
      pattern: "Cors__AllowedOrigins"
    - from: "backend/ShareService.Api.Tests/HealthTests.cs"
      to: "backend/ShareService.Api/Program.cs"
      via: "WebApplicationFactory calls the real /health route"
      pattern: "WebApplicationFactory"
---

<objective>
Create the backend scaffold and automated validation needed for fail-fast, environment-driven startup.

Purpose: Establish the .NET foundation before Docker wiring so later plans build on a tested startup contract instead of guessing at config behavior.
Output: ASP.NET Core 8 API scaffold, Dockerfile, and xUnit coverage for config + health behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@README.md
@index.html
@app.js

<interfaces>
From index.html:
```html
<input id="api-base-url" type="text" placeholder="API base URL (e.g. http://localhost:5000)">
<input id="api-jwt-token" type="password" placeholder="JWT token">
```

From app.js:
```javascript
const res = await fetch(`${baseUrl}/documents`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": `Bearer ${token}`
  },
  body: JSON.stringify(payload)
});
```

Honor these contracts now:
- Keep the API host aligned with `http://localhost:5000` once Docker is added.
- Do not add a login endpoint or move the existing frontend files in Phase 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the API project, Docker build, and test host</name>
  <files>
backend/ShareService.Api/ShareService.Api.csproj
backend/ShareService.Api/Dockerfile
backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj
  </files>
  <read_first>
.planning/phases/01-foundation/01-CONTEXT.md
.planning/phases/01-foundation/01-RESEARCH.md
.planning/phases/01-foundation/01-VALIDATION.md
README.md
index.html
backend/ShareService.Api/ShareService.Api.csproj
backend/ShareService.Api/Dockerfile
backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj
  </read_first>
  <action>Create a new ASP.NET Core 8 Minimal API project at `backend/ShareService.Api` and a matching xUnit project at `backend/ShareService.Api.Tests`. Add `MongoDB.Driver` to the API project. Add `Microsoft.AspNetCore.Mvc.Testing` and `Mongo2Go` to the test project so `dotnet test` can exercise `/health` without requiring Docker. Create `backend/ShareService.Api/Dockerfile` for a multi-stage .NET 8 build that exposes container port `8080`. Keep the current root frontend files untouched, do not add nginx, and do not add a login/auth token endpoint in this phase.</action>
  <acceptance_criteria>
- `backend/ShareService.Api/ShareService.Api.csproj` contains `<TargetFramework>net8.0</TargetFramework>`.
- `backend/ShareService.Api/ShareService.Api.csproj` contains `MongoDB.Driver`.
- `backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj` contains `Microsoft.AspNetCore.Mvc.Testing`.
- `backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj` contains `Mongo2Go`.
- `backend/ShareService.Api/Dockerfile` contains `EXPOSE 8080`.
- `backend/ShareService.Api/Dockerfile` contains `dotnet publish`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet restore backend/ShareService.Api/ShareService.Api.csproj &amp;&amp; dotnet restore backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj</automated>
  </verify>
  <done>Both .NET projects restore successfully and the Dockerfile is ready for the later Compose plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-fast env config, UseStaticFiles, and Mongo-backed health coverage</name>
  <files>
backend/ShareService.Api/Program.cs
backend/ShareService.Api.Tests/HealthTests.cs
backend/ShareService.Api.Tests/ConfigTests.cs
  </files>
  <read_first>
.planning/PROJECT.md
.planning/STATE.md
.planning/phases/01-foundation/01-CONTEXT.md
.planning/phases/01-foundation/01-RESEARCH.md
.planning/phases/01-foundation/01-VALIDATION.md
index.html
app.js
backend/ShareService.Api/Program.cs
backend/ShareService.Api.Tests/HealthTests.cs
backend/ShareService.Api.Tests/ConfigTests.cs
  </read_first>
  <behavior>
    - Test 1: `GET /health` returns HTTP 200 and JSON containing `"status":"ok"` when `Mongo__ConnectionString`, `Mongo__DatabaseName`, `Auth__JwtSecret`, and `Cors__AllowedOrigins` are supplied and Mongo is reachable.
    - Test 2: Startup throws `InvalidOperationException` with `Missing required config: Auth__JwtSecret` when that env var is absent.
    - Test 3: Startup throws `InvalidOperationException` with `Missing required config: Cors__AllowedOrigins` when that env var is absent.
  </behavior>
  <action>Write the tests first, then implement `backend/ShareService.Api/Program.cs` to satisfy them. In startup, add a local `Require(string key)` helper that throws `InvalidOperationException($"Missing required config: {key}")`; require `Mongo__ConnectionString`, `Mongo__DatabaseName`, `Auth__JwtSecret`, and `Cors__AllowedOrigins`; split `Cors__AllowedOrigins` on commas with trimmed entries; register a named CORS policy called `Frontend`; register `IMongoClient` with `new MongoClient(mongoConnectionString)`; call `app.UseCors("Frontend")`; call `app.UseStaticFiles()` now to honor the locked 2-service architecture without moving `index.html`, `app.js`, or `styles.css` yet; add `GET /health` that pings the `admin` database with `{ ping: 1 }` and returns `Results.Ok(new { status = "ok" })`. In `HealthTests.cs`, use `Mongo2Go` plus `WebApplicationFactory` to boot the real app with temporary env vars and assert the route succeeds. In `ConfigTests.cs`, boot the app with missing env vars and assert the exact fail-fast exception messages. Do not hardcode any secret fallback in code, Docker, or appsettings.</action>
  <acceptance_criteria>
- `backend/ShareService.Api/Program.cs` contains `Missing required config:`.
- `backend/ShareService.Api/Program.cs` contains `Mongo__ConnectionString`.
- `backend/ShareService.Api/Program.cs` contains `Mongo__DatabaseName`.
- `backend/ShareService.Api/Program.cs` contains `Auth__JwtSecret`.
- `backend/ShareService.Api/Program.cs` contains `Cors__AllowedOrigins`.
- `backend/ShareService.Api/Program.cs` contains `app.UseStaticFiles();`.
- `backend/ShareService.Api/Program.cs` contains `app.MapGet("/health"`.
- `backend/ShareService.Api.Tests/HealthTests.cs` contains `Mongo2Go`.
- `backend/ShareService.Api.Tests/ConfigTests.cs` contains `Missing required config: Auth__JwtSecret`.
- `backend/ShareService.Api.Tests/ConfigTests.cs` contains `Missing required config: Cors__AllowedOrigins`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj --filter "FullyQualifiedName~HealthTests|FullyQualifiedName~ConfigTests"</automated>
  </verify>
  <done>The API startup contract is environment-driven, `/health` is meaningful, and the xUnit suite proves config failures and healthy startup behavior.</done>
</task>

</tasks>

<verification>
Run `dotnet test backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj` after each task commit. The plan is complete only when config-missing tests fail with the exact required messages and the healthy path returns 200 from `/health`.
</verification>

<success_criteria>
- `backend/ShareService.Api` exists as a dedicated backend subdirectory.
- No secret defaults are committed in source or Docker files.
- `/health` proves Mongo connectivity rather than returning a hardcoded 200.
- The backend foundation is ready for root Compose wiring in `01-02-PLAN.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
