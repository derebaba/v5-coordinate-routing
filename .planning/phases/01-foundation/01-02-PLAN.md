---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .env.example
  - .gitignore
  - docker-compose.yml
  - README.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Developers can start the full stack with `docker compose up --build` and reach the API at http://localhost:5000."
    - "MongoDB data survives `docker compose down` followed by `docker compose up` because persistence is provided by a named volume."
    - "Secrets and allowed origins live in a root `.env` workflow, while `.env.example` stays safe to commit."
  artifacts:
    - path: ".env.example"
      provides: "Placeholder-only environment contract for local secrets and allowed origins"
      contains: "Auth__JwtSecret="
    - path: ".gitignore"
      provides: "Protection against committing local `.env` secrets"
      contains: ".env"
    - path: "docker-compose.yml"
      provides: "Two-service local stack with api + mongo and named persistence volume"
      contains: "mongo_data:/data/db"
    - path: "README.md"
      provides: "Operator instructions for start, rebuild, logs, stop, and destructive reset"
      contains: "docker compose up --build"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/ShareService.Api/Dockerfile"
      via: "api service build.dockerfile"
      pattern: "backend/ShareService.Api/Dockerfile"
    - from: "docker-compose.yml"
      to: "MongoDB persistent storage"
      via: "mongo service mounts mongo_data to /data/db"
      pattern: "mongo_data:/data/db"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Documented commands match the actual compose entrypoints"
      pattern: "docker compose"
---

<objective>
Add the root runtime artifacts that make the new backend runnable, persistent, and easy to operate locally.

Purpose: Deliver the actual Phase 1 developer workflow — safe env handling, one-command startup, and documented reset semantics.
Output: `.env.example`, `.gitignore`, `docker-compose.yml`, and README instructions that satisfy OPS-01/02/03.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@backend/ShareService.Api/Program.cs
@backend/ShareService.Api/Dockerfile
@README.md
@index.html
@app.js

<interfaces>
From index.html:
```html
<button id="upload-to-api" type="button">Upload to API</button>
<input id="api-base-url" type="text" placeholder="API base URL (e.g. http://localhost:5000)">
```

From app.js:
```javascript
const baseUrl = (el.apiBaseUrl.value || localStorage.getItem("api_base_url") || "")
  .trim()
  .replace(/\/$/, "");
```

Compose must preserve this host contract:
- API available from the host at `http://localhost:5000`
- Mongo kept internal to Docker for the normal workflow
- No nginx container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root env contract and two-service Compose stack</name>
  <files>
.env.example
.gitignore
docker-compose.yml
  </files>
  <read_first>
.planning/STATE.md
.planning/phases/01-foundation/01-CONTEXT.md
.planning/phases/01-foundation/01-RESEARCH.md
index.html
app.js
backend/ShareService.Api/Program.cs
backend/ShareService.Api/Dockerfile
.env.example
.gitignore
docker-compose.yml
  </read_first>
  <action>Create a root `.env.example` with placeholders only for `Mongo__ConnectionString`, `Mongo__DatabaseName`, `Auth__JwtSecret`, and `Cors__AllowedOrigins`. Use `mongodb://mongo:27017` as the host portion in the placeholder connection string so the API container talks to the Docker service name, not `localhost`. Add `.env` to `.gitignore` exactly so local secrets are never committed. Create `docker-compose.yml` with only two services: `api` and `mongo`. The `api` service must build from repo root using `backend/ShareService.Api/Dockerfile`, load variables from `.env`, and map host port `5000` to container port `8080`. The `mongo` service must use the `mongo:7` image, stay internal only with no host port mapping, and mount the named volume `mongo_data:/data/db`. If a local `.env` file does not exist during execution, copy `.env.example` to `.env`, replace the JWT placeholder with a non-placeholder test secret, and keep `.env` untracked.</action>
  <acceptance_criteria>
- `.env.example` contains `Mongo__ConnectionString=mongodb://mongo:27017`.
- `.env.example` contains `Mongo__DatabaseName=coordinate-routing`.
- `.env.example` contains `Auth__JwtSecret=`.
- `.env.example` contains `Cors__AllowedOrigins=`.
- `.gitignore` contains a line that is exactly `.env`.
- `docker-compose.yml` contains `backend/ShareService.Api/Dockerfile`.
- `docker-compose.yml` contains `5000:8080`.
- `docker-compose.yml` contains `mongo:7`.
- `docker-compose.yml` contains `mongo_data:/data/db`.
- `docker-compose.yml` does not contain `27017:27017`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>The repo has a safe root env workflow and a valid two-service Compose file matching the locked local topology.</done>
</task>

<task type="auto">
  <name>Task 2: Document the local run, health-check, and persistence workflows</name>
  <files>README.md</files>
  <read_first>
.planning/phases/01-foundation/01-CONTEXT.md
.planning/phases/01-foundation/01-VALIDATION.md
docker-compose.yml
backend/ShareService.Api/Program.cs
README.md
  </read_first>
  <action>Update `README.md` with a dedicated section for the share-service local stack. Include the exact workflow to copy `.env.example` to `.env`, run `docker compose up --build`, verify `http://localhost:5000/health`, stream logs with `docker compose logs -f api`, stop with `docker compose down`, and fully reset persistent Mongo data with `docker compose down -v`. State explicitly that `docker compose down` preserves the named `mongo_data` volume while `down -v` deletes it. Document that MongoDB is intentionally internal-only in Phase 1. Include the persistence probe command from `01-VALIDATION.md` so developers can prove data survives a restart without custom scripts.</action>
  <acceptance_criteria>
- `README.md` contains `docker compose up --build`.
- `README.md` contains `http://localhost:5000/health`.
- `README.md` contains `docker compose logs -f api`.
- `README.md` contains `docker compose down -v`.
- `README.md` contains `mongo_data`.
- `README.md` contains `docker exec $(docker compose ps -q mongo) mongosh`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test backend/ShareService.Api.Tests/ShareService.Api.Tests.csproj &amp;&amp; docker compose up -d --build &amp;&amp; powershell -Command "try { $r = Invoke-WebRequest http://localhost:5000/health -UseBasicParsing; if ($r.StatusCode -ne 200) { exit 1 }; docker exec $(docker compose ps -q mongo) mongosh --quiet --eval \"db.getSiblingDB('coordinate-routing').phase1.insertOne({probe:'ok'})\"; docker compose down; docker compose up -d; $doc = docker exec $(docker compose ps -q mongo) mongosh --quiet --eval \"EJSON.stringify(db.getSiblingDB('coordinate-routing').phase1.findOne({probe:'ok'}))\"; if ($doc -notmatch 'probe') { exit 1 } } finally { docker compose down }"</automated>
  </verify>
  <done>The README explains the exact local lifecycle and the automated smoke command proves stack startup plus Mongo persistence across restart.</done>
</task>

</tasks>

<verification>
Run the full validation contract from `01-VALIDATION.md` after the plan completes: `dotnet test`, then the Docker smoke check, then the Mongo persistence probe. Do not mark the plan done if the API only boots without the documented workflow.
</verification>

<success_criteria>
- `docker compose up --build` is the primary documented startup path.
- API is reachable from the host at `http://localhost:5000`.
- MongoDB is not exposed to the host by default.
- `docker compose down` preserves data and `docker compose down -v` removes it.
- `.env.example` is safe to commit and `.env` is ignored.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
